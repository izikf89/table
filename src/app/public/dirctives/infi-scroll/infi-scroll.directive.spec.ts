import { InfiScrollDirective } from './infi-scroll.directive';

describe('InfiScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiScrollDirective();
    expect(directive).toBeTruthy();
  });
});
