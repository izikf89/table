import { ActiveRowDirective } from './active-row.directive';

describe('ActiveRowDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveRowDirective();
    expect(directive).toBeTruthy();
  });
});
